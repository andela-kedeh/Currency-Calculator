<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wecking.currencycalculator" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.checkpoint3.wecking.CurrencyCalculatorMain.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.checkpoint3.wecking.CurrencyCalculatorConverter.Converter"
            android:label="@string/title_activity_converter">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.checkpoint3.wecking.CurrencyCalculatorMain.MainActivity" />
        </activity>
    </application>

</manifest>
